# Rebuttal ablation: chan_independent — launch like base.yaml (hydra root); 50k steps.
defaults:
  - /data: audioset_ambisonics
  - /optimizer: adamW
  - /trainer: default_trainer
  - /patching: frame        # 200-token grid; budget rule in masking/*.yaml
  - /masking: random
  - _self_

model: AudioSphereChannelMasked
model_size: base
save_dir: /projects/0/prjs1338/ablations/chan_independent
use_mwmae_decoder: True
ckpt_path: null
seed: 42
channel_mask_mode: independent
add_mask_indicator: True
